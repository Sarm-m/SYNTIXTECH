# Frontend example. Copy to apps/web/.env.local for local frontend work.
# Only VITE_* values are consumed by Vite and exposed to the browser.
VITE_API_URL=/api
VITE_GOOGLE_CLIENT_ID=your-google-oauth-client-id
VITE_ENABLE_LOCAL_AUTH_FALLBACK=false
